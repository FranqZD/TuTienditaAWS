{
  "name": "tu-tiendita-lambda-checkout",
  "version": "1.0.0",
  "description": "Lambda function for processing checkout in Tu Tiendita",
  "main": "index.js",
  "scripts": {
    "test": "vitest --run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "fast-check": "^3.22.0"
  }
}
